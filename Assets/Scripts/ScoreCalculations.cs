using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScoreCalculations : MonoBehaviour
{

    // GLOSSARY:

    // singleHabitantHappiness = porcentaje de felicidad de habitante unico
    // goodNotes = notas de la partitura interpretadas correctamente
    // totalNotes = notas totales de la partitura

    private float singleHabitantHappiness;
    private int goodNotes;
    private int totalNotes;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    
}
